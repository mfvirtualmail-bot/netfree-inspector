# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node (not currently used, but kept for safety if build tools are added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/
*.tgz

# Local environment
.env
.env.*
!.env.example

# Local research scratch (real recordings — contain account identity data, never commit)
.local-research/
